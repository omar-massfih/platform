apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: garage-data # object storage for the ingest landing zone
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
