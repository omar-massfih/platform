# In-cluster only — no Ingress. ingest's Deployment reaches this at
# garage.platform.svc.cluster.local:3900.
apiVersion: v1
kind: Service
metadata:
  name: garage
spec:
  selector: { app: garage }
  ports:
    - name: s3
      port: 3900
      targetPort: 3900
