# omar VM, reached over the VPN. Matches the `omar` entry in ~/.ssh/config
# (HostName 10.8.0.1, User omar, IdentityFile ~/.ssh/id_ed25519).
[k3s]
omar ansible_host=10.8.0.1 ansible_user=omar

[k3s:vars]
ansible_ssh_private_key_file=~/.ssh/id_ed25519
