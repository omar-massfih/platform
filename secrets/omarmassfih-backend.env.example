# Optional chat/RAG credential for the public notes backend. Copy to
# omarmassfih-backend.env and run the secrets Ansible playbook. Notes serving and
# Postgres seeding work without it; chat and chunk embedding require it.
AI_GATEWAY_API_KEY=
